# 实施进度笔记(2026-07-07)

## 已完成
- Phase 1 v8 迁移收尾:types.ts(BOTTLE_CATEGORIES 换 8 材料分类+DEFAULT_MATERIAL_CATEGORIES+EN映射+BOTTLE_STYLES+Bottle.formFactors 可选字段)、
  waldorf-ingredients.ts(mapCategory→migrateMaterialBottleV8)、seed.ts(全部旧"原材料"→新分类)、classify.ts(材料归类走 migrateMaterialBottleV8)、
  homemade/cost.ts(matchMaterialBottle 用 bottleGroupOf)、bottles.tsx(handleAdd 不再写死"原材料")、taxonomy.tsx groupOf fallback→staticBottleGroupOf。
  store.tsx 已有 v8 一次性迁移(此前完成)。taxonomy.tsx 已有 migrateMaterialBottleV8/migrateCategoryDefsV8/migrateStyleDefsV8(此前完成)。
- Phase 2 形态折叠引擎:lib/recipes/form-fold.ts(stripForm/FORM_DEFS/parseFormCount/formCost/defaultPieceGrams/formFactorOf,
  Bottle.formFactors 可覆盖系数,_pieceGrams 覆盖单件克重)。smart-link.ts 第6级形态折叠匹配(SmartLink bottle 分支带 form)。
  smart-cost.ts formInfo 分支。tests/form-fold.test.ts 10 tests 全过。
- Phase 3 装饰拆分:lib/recipes/garnish-split.ts(splitGarnish/estimateGarnishCost;or 取高、and/与/及累加、顿号并列)。

## 待做
1. ✅ auto-add.ts 完成(draftFromName/splitCompoundName/buildAutoAddDrafts)
2. ✅ store.tsx splitBottleDraft:addBottle 自动拆分连接词名称
3. ✅ recipe/[id].tsx:garnishCost 组渲染(or 标注/formInfo 副文本/自动入库 useEffect)
4. 剩余:全量测试 + todo.md 勾选 + checkpoint + 交付

## 关键 API 备忘
- bottles store: lib/bottles/store.tsx → useBottles() 提供 bottles, addBottle(b: Bottle), updateBottle...(需确认签名)
- classifyIngredient(name) → { library, name, nameAlt, category, style?, confidence }
- migrateMaterialBottleV8({category:"原材料", name, nameZh}) → { category, style } | null
- estimateGarnishCost(garnish, bottles, preps) → { groups, total, unmatchedNames }
- i18n: lib/i18n 的 t() key 需在 zh/en 字典添加 detail.cost.garnish 等(需确认文件位置 grep "detail.cost.total")
